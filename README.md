simplemappingsystem-tests
=========================

Written in python to test your simplemappingsystem server impl through the JSON-RPC layer. Swap out your server impl and run the tests - all must pass!


Running the tests
-----------------

nosetests -v tests.py